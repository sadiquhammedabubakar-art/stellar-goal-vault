# Baseline Costs

Storage: see CONTRACT_ABI.md. Keys: NextCampaignId (instance), Campaign(id) (persistent), Contribution(id,addr) (persistent). Budget: 100/1000 -> 1100 entries. Migration: reinit counter; schema changes need migration.